# Cloudflare Worker config for the Sift OAuth token-exchange proxy.
# Deploy: `wrangler deploy`. Set secrets (do NOT put them here):
#   wrangler secret put NOTION_CLIENT_ID
#   wrangler secret put NOTION_CLIENT_SECRET
name = "sift-oauth-proxy"
main = "worker.js"
compatibility_date = "2025-09-03"
